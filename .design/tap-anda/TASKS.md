# Build Tasks: Óleńsiz — итерация 3

Generated from: `.design/tap-anda/DESIGN_BRIEF.md`
Date: 2026-09-07

Порядок тот же, что и в прошлые итерации: сначала риск, потом фундамент, потом
видимое, потом деплой. Самый рискованный кусок здесь — снова каталог: правка
слияния артистов могла выбросить из игры настоящих исполнителей, и ровно это
и произошло с первой попытки (см. `DECISIONS.md` §13).

Все задачи выполнены. Отметки проставлены по факту, а не заранее.

---

## Риск (делать первым)

- [x] **Блок I1. Убрать склейку артистов по имени.** Сопоставление только по
      `artistId`, короткие алиасы не опознают артиста.
      _Настоящей причиной бага «Ирины Кайратовны» оказался алиас `IK`: две буквы
      совпадают с кем угодно. Алиасы короче 4 символов исключены из опознания._
- [x] **Проверить, что строгое правило не выбрасывает своих.** _Выбросило:
      Мақпал Жүнісова, Нұрлан Өнербаев, Алтынай Жорабаева, Батырхан Шүкенов,
      Адаптация — у всех по несколько настоящих профилей в Apple. Правило
      переделано: профили сливаются, но каждый проходит тот же тест на
      подлинность, и каждое слияние выписано в отчёт сборки._
- [x] **Блок I2. Категории.** Тонкие жанры слиты в родительские, порог показа
      25 треков. _Скрыты `qpop` (22) и `patriotic` (21); показываются 9 из 11._
- [x] **Пересобрать каталог на `artists-v2.csv`.** _661 трек, 130 исполнителей.
      Дубли строк в CSV схлопываются сборщиком — править CSV руками не нужно._

## Фундамент

- [x] **Блок E. Переписать систему очков.** Бонус только добавляет (≤25%),
      жёсткий пол 10%, τ = 6 с, множитель режима. `SCORING.md` переписан целиком
      с примерами для крайних случаев. Done = `verify:scoring` зелёный.
- [x] **Блок B1. Два режима подачи.** Ступени, цены и множитель живут в
      `MODES` в `src/scoring.js`; экспертный дополнительно подмешивает тир 5 и
      андеграунд в верхние уровни.
- [x] **Блок I3. Без повторов.** `PlayHistory` в localStorage, сброс истории
      только исчерпанного уровня, потолок 1200 id.
      Done = 20 партий подряд без единого повтора (проверяется в `verify:search`).
- [x] **Блок A. Ребрендинг и домен.** Óleńsiz везде: title, og, JSON-LD,
      логотип, документация, ключи localStorage.
- [x] **Токены: одна шкала отступов.** 4/8/12/16/24/32/48/64 и ничего между.
      Сетка 12 колонок объявлена в токенах.

## Видимое

- [x] **Блок C1. Стартовый экран пересобран.** Две зоны сетки: слева герой с
      кнопкой старта и панель настроек под ним, справа — лидерборды (или
      «как это работает», пока таблицы нет). Обе строки по содержимому, блок
      центрируется целиком — так ушла дыра между героем и настройками.
- [x] **Блок C2. Игровой экран по той же сетке.** Центральная колонка 7 из 12,
      вертикальный ритм из шкалы.
- [x] **Блок C3. og:image переделан.** Крупный `Óleńsiz`, сетка обложек,
      одна кнопка. 1200×630, `npm run og`.
- [x] **Блок D1. Ответ по «Тексеру».** Выбор из выдачи заряжает кнопку,
      очистка поля возвращает её в «Өткізу».
- [x] **Блок D2. Обратная связь на промах.** Вспышка 440 мс с затуханием,
      серый = пропуск, красный = была попытка.
- [x] **Блок D3. Отвергнутые варианты** зачёркнуты, приглушённо-красные,
      выбрать нельзя.
- [x] **Блок D4. Клавиатура на телефоне** открывается только по тапу в поле.
- [x] **Блок E3. Цены ступеней убраны из UI**, вместо них убывающий столбик.
- [x] **Блок F. Фон ожил.** Три пятна с независимыми траекториями (19/23/29 с),
      реакция на бас со сглаживанием и потолком дельты, при
      `prefers-reduced-motion` — статичный градиент.
- [x] **Блок G1. Финал: шаг, время, очки** на каждой карточке.
- [x] **Блок G2. Карточки кликабельны** — открывается панель со ссылками
      на Apple Music / Spotify / Deezer.
- [x] **Блок H. Лидерборды по срезам.** Период × категория × режим × возраст,
      гостевые имена `Qonaq N`, место в своём срезе, один перехват при уходе
      без имени, имя запоминается.
- [x] **Блок H4. Шеринг** дополнен режимом и категорией. Сетка эмодзи не
      тронута (блок K).

## Инструменты

- [x] **Блок J. Локальный редактор каталога.** `npm run editor` →
      `scripts/editor-server.mjs`, правки в `data/overrides.json`, сборщик
      накладывает их поверх и никогда не затирает.
- [x] **Волна и точка старта.** Превью декодируется в браузере, пики рисуются
      на canvas, маркер тащится мышью, результат слышно сразу.
- [x] **Фильтры редактора**: текст, категория, уровень, возраст,
      «только непроверенные», «только с флагами». Прогресс на виду.

## SEO и деплой

- [x] **Блок A. SEO с нуля.** hreflang, JSON-LD `VideoGame`, sitemap, robots,
      статический текст, `about.html`, `SEO.md` с чеклистом ручных шагов.
- [x] **Новый домен `olensiz.zhengisbay.com`** — CNAME без прокси,
      сертификат GitHub выпущен, HTTPS принудительный.
- [x] **301 со старого домена** — `songless` под прокси Cloudflare плюс
      Single Redirect с сохранением пути и query.
- [x] **Проверки перед сдачей**: `verify:scoring`, `verify:search`,
      `verify:contrast`, `verify:noscroll` — все зелёные.

## Не трогали (блок K)

- Механика пяти раундов и порядок уровней 1→5.
- Анимация закрутки карточек на финале.
- Формат шеринга — добавлена одна строка, сетка эмодзи прежняя.
- Атрибуция артистов.
- Только тёмная тема, ноль скроллинга.
