# Build Tasks: ТАП ӘНДІ — итерация 2

Generated from: `.design/tap-anda/DESIGN_BRIEF.md`
Date: 2026-09-06

Это переработка существующего проекта, а не сборка с нуля. Порядок тот же, что
и в первой итерации: сначала риск, потом фундамент, потом видимое. Самый
рискованный кусок здесь — каталог: если бы whitelist по `artistId` не отсекал
чужих артистов, всё остальное строилось бы вокруг испорченных данных.

Все задачи выполнены. Отметки проставлены по факту.

## Риск (делать первым)

- [x] **Переписать сбор каталога на whitelist по `artistId`**: имя из CSV →
      `artistId` → `lookup`, свободный текстовый поиск по названиям убрать
      полностью. Done = ни одного запрещённого артиста из блока A.
      _Переписан `scripts/build-catalog.mjs`, `scripts/seeds.mjs` удалён._
- [x] **Разрешение однофамильцев**: проверить на реальных данных, что whitelist
      по ID достаточен. _Оказалось — нет: `Junior` дал испанские треки, `Duman`
      турецкие, `ZAQ` японские. Добавлено подтверждение артиста по хитам из
      `notes` и доле кириллицы. См. `DECISIONS.md` §1._
- [x] **Предзагрузка партии**: декодировать все пять треков до старта, чтобы
      первое нажатие «Ойнату» давало звук, а не тишину. Done = замер времени
      старта воспроизведения. _Замерено: 5 треков ≈ 3,4 с, звук за ~70 мс._

## Фундамент

- [x] **Уровни из `fame_tier`**: убрать эвристику `obscurity`, соответствие
      уровень→тиры хранить в собранном JSON. _`levelTiers` в `tracks.json`,
      `Catalog.poolForLevel`._
- [x] **Ступени 0,1 с**: поправить `STEP_MS[0]`, не трогая формулу очков
      (блок H). Done = `verify:scoring` зелёный. _Заодно упрощён детектор:
      отдельный «фикс первой миллисекунды» из итерации 1 больше не нужен._
- [x] **Токены: только тёмная тема, только десктоп**: удалить светлую палитру,
      мобильные брейкпоинты, добавить размеры от высоты вьюпорта и пороги
      деградации. _`styles/tokens.css` переписан._
- [x] **Два языка**: `i18n/kk.json` + `ru.json`, смена языка на лету,
      сохранение выбора. Done = ключи в двух файлах совпадают (134/134).
- [x] **Деградация по высоте**: `src/fit.js` — наблюдатель `innerHeight`,
      атрибут `data-dense` 0..4, пороги из токенов.

## Основной интерфейс

- [x] **Стартовый экран в две колонки**: слева герой, факты, режимы и старт;
      справа два лидерборда. Текстовую подсказку про очки убрать, правила —
      в оверлей за «?».
- [x] **Режимы и фильтры**: возрастной сегмент, жанровые чипы из каталога,
      Random по умолчанию, блокировка старта при недостаточной выборке.
      Покрывает G1–G4.
- [x] **Экран загрузки партии**: прогресс-бар и счётчик, партия ждёт все пять.
- [x] **Экран раунда без скролла**: плеер, шкала ступеней, поиск, текущий счёт.
      Done = ничего из обязательного не прячется ни при какой плотности.
- [x] **Анимация поиска**: плавное раскрытие, ступенчатая задержка 25 мс,
      подсветка совпавшей подстроки, мягкое исчезновение. Покрывает D3.
- [x] **Карточка результата раунда**: перекомпонована горизонтально
      (обложка слева, метаданные справа), чтобы поместиться по высоте.
- [x] **Финальный экран**: логотип убран, шапка сжата, карточки уменьшены;
      в один экран влезают счёт, разбивка, 5 карточек, ник, отправка, шеринг
      и «Тағы ойнау». Покрывает C4.

## Взаимодействия и эффекты

- [x] **Цвет и пульсация под музыку**: `src/palette.js` (доминирующие цвета
      обложки через canvas) + `src/pulse.js` (`AnalyserNode`, 20–200 Гц).
      Покрывает D1. _Понадобились послабляющие проходы квантизации: на
      чёрно-белых обложках строгий фильтр не оставлял ни одного пикселя._
- [x] **Усиленный 3D на финале**: больше `translateZ`, масштаб и тень;
      соседние карточки отодвигаются и притухают. Покрывает D2.
- [x] **Подтверждение выхода по клику на логотип**: только во время партии.
      Покрывает D4.
- [x] **Два лидерборда**: компактные таблицы на старте, полный список в
      оверлее, отправка результата с финала. Покрывает E1–E2.
- [x] **Apps Script с двумя топами**: `doGet` отдаёт `allTime` и `today` одним
      ответом, «сегодня» считается по серверному времени. Покрывает E3.
- [x] **Фильтр мата и санитизация ника**: на клиенте и на сервере.
      _Порядок операций пришлось поправить: `b1tch` проходил фильтр._

## Полировка и проверки

- [x] **Ноль скроллинга**: `overflow: hidden`, `100dvh`, `clamp()`, деградация.
      Покрывает C3.
- [x] **Размещение выдачи поиска**: направление и высота по свободному месту,
      чтобы не закрывать плеер и не обрезаться краем экрана.
- [x] **og:image 1200×630**: сетка обложек, крупная типографика, одна кнопка.
      Покрывает C5. _`scripts/make-og.mjs`._
- [x] **Проверка «ноль скроллинга»**: `scripts/verify-noscroll.mjs` — шесть
      высот × четыре экрана, плюс видимость обязательных элементов.
- [x] **Проверка контраста**: обновлена под одну тему, плюс падает, если в
      токенах остались следы светлой.
- [x] **Проверка каталога**: уровни из тиров, фильтры, защита от пустой
      выборки, отсутствие запрещённых артистов, объём 500–900.

## Ревью

- [x] **Design review**: скриншоты всех экранов на двух разрешениях, анализ,
      исправления. _`scripts/screenshots.mjs`, результат — `DESIGN_REVIEW.md`._
